/**
 * getMonthFromDate - this method accept date and return month for this date 
*/
const getMonthFromDate = (date: Date) => {

}

/**
 * getMonthName - this method accept date and return month name from date
*/
const getMonthName = (date: Date) => {

}

/**
 * getNextMonth - this method accept currentMonth and return next month Date obj
*/
const getNextMonth = (currentMonth: Date) => {

}

/**
 * getPrevMonth - this method accept currentMonth and return prev month Date obj
*/
const getPrevMonth = (currentMonth: Date) => {

}

/**
 * getDays - this method accept currentMonth and return array of days for this month
*/
const getDays = (currentMonth: Date) => {

}

/**
 * getDate - this method accepts day and return day name
*/
const getDate = (day: Date) => {}

/**
 * inCurrentMonth - accepts day date and chek if this date is in currentMonth
*/
const inCurrentMonth = (day: Date, currentMonth: Date) => {}

/**
 * inPast - accept day date and check if this date begore today
*/
const inPast = (day: Date) => {}

/**
 * isFirstOrLastDay - accept day date, minDate and maxDate, check if this date is equele to minDate or maxDate
*/
const isFirstOrLastDay = (day: Date, minDate: Date, maxDate: Date) => {}

/**
 * inRange - accept day date, minDate and maxDate, check if this date is between minDate or maxDate
*/
const inRange = (day: Date, minDate: Date, maxDate: Date) => {}

/**
 * dayIsEveryWeek - accepts day date and check if this day is in a sameMonth with selectedDate, day is the same day like selectedDate 
*/
const dayIsEveryWeek = () => {}

/**
 * dayIsEveryMonth - accepts day date and selectedDate, check if this day is in next month from selectedDate and this day is the same like selectedDate
*/
const dayIsEveryMonth = (day: Date, selectedDate: Date) => {}